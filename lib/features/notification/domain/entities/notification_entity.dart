
class NotificationEntity{
  String title;
  String id;
  String description;
  String createdAt;
  bool isRead;

  NotificationEntity({required this.title,required this.id, required this.description,required this.createdAt,required this.isRead });
}